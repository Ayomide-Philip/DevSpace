# DevSpace API

## Overview
This is a backend service for the DevSpace application, built with Next.js App Router API Routes. It handles user authentication and data management using Mongoose as the ODM for a MongoDB database and NextAuth.js for session and credential management.

## Features
- **Next.js**: API routing and server-side logic.
- **Mongoose**: Object Data Modeling (ODM) for MongoDB.
- **NextAuth.js**: Credential-based authentication and session management.
- **bcrypt**: Secure password hashing.

## Getting Started
### Installation
1.  **Clone the repository:**
    ```bash
    git clone https://github.com/Ayomide-Philip/DevSpace.git
    cd DevSpace
    ```

2.  **Install dependencies:**
    ```bash
    npm install
    ```

3.  **Set up environment variables:**
    Create a `.env.local` file in the root of the project and add the necessary variables.
    ```bash
    touch .env.local
    ```

4.  **Run the development server:**
    ```bash
    npm run dev
    ```
    The application will be available at `http://localhost:3000`.

### Environment Variables
All required environment variables must be placed in a `.env.local` file in the project root.

-   `MONGO_DB_URL`: The connection string for your MongoDB database.
    ```
    MONGO_DB_URL=mongodb+srv://<user>:<password>@cluster0.mongodb.net/DevspaceNextjs
    ```
-   `AUTH_SECRET`: A secret key used by NextAuth.js for signing tokens. You can generate one using `openssl rand -base64 32`.
    ```
    AUTH_SECRET=your_super_secret_key_here
    ```

## API Documentation
### Base URL
`http://localhost:3000`

### Endpoints
#### GET /api
A test endpoint to confirm the API is running.

**Request**:
No payload required.

**Response**:
```json
{
  "message": "Hello from DevSpace Api route"
}
```

**Errors**:
- None

#### POST /api/auth/register
Registers a new user in the database.

**Request**:
```json
{
  "name": "John Doe",
  "username": "johndoe",
  "email": "john.doe@example.com",
  "password": "a_strong_password123"
}
```

**Response**:
A success message and the new user's ID.
```json
{
  "message": "User created Successfully",
  "user": {
    "id": "60d0fe4f5311236168a109ca"
  }
}
```

**Errors**:
-   `400 Bad Request`: "Please enter a your name"
-   `400 Bad Request`: "Please enter at least 5 characters for your name"
-   `400 Bad Request`: "Please enter a valid email address"
-   `400 Bad Request`: "Email already exists"
-   `400 Bad Request`: "Username already exists"
-   `400 Bad Request`: "Something went wrong while creating an account for you"

[![Readme was generated by Dokugen](https://img.shields.io/badge/Readme%20was%20generated%20by-Dokugen-brightgreen)](https://www.npmjs.com/package/dokugen)